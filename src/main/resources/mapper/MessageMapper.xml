<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="icu.xindongxuanxiang.game.mapper.MessageMapper">

    <resultMap id="messageMapper" type="icu.xindongxuanxiang.game.model.entity.Message">
        <id property="id" column="id"/>
        <result property="userId" column="userId"/>
        <result property="content" column="content"/>
        <result property="deleted" column="isDeleted"/>
        <result property="top" column="isTop"/>
        <result property="reviewStatus" column="reviewStatus"/>
        <result property="createdAt" column="createdAt" javaType="java.time.LocalDateTime"/>
        <result property="updatedAt" column="updatedAt" javaType="java.time.LocalDateTime"/>
    </resultMap>

    <select id="getMessage" resultMap="messageMapper" parameterType="java.lang.Integer">
        SELECT `id`, `userId`, `content`, `isDeleted`, `isTop`, `reviewStatus`, `createdAt`, `updatedAt`
        FROM Messages
        WHERE id = #{id}
          AND isDeleted = 0
          AND reviewStatus = 'APPROVED'
    </select>

    <select id="getMessageRaw" resultMap="messageMapper" parameterType="java.lang.Integer">
        SELECT `id`, `userId`, `content`, `isDeleted`, `isTop`, `reviewStatus`, `createdAt`, `updatedAt`
        FROM Messages
        WHERE id = #{id}
          AND isDeleted = 0
    </select>

    <select id="getAllMessages" resultMap="messageMapper">
        SELECT `id`, `userId`, `content`, `isDeleted`, `isTop`, `reviewStatus`, `createdAt`, `updatedAt`
        FROM Messages
        WHERE isDeleted = 0
          AND reviewStatus = 'APPROVED'
        ORDER BY isTop DESC, createdAt DESC
    </select>

    <select id="getAllMessagesAdmin" resultMap="messageMapper">
        SELECT `id`, `userId`, `content`, `isDeleted`, `isTop`, `reviewStatus`, `createdAt`, `updatedAt`
        FROM Messages
        WHERE isDeleted = 0
        ORDER BY isTop DESC, createdAt DESC
    </select>

    <select id="getMessagesByUserId" resultMap="messageMapper" parameterType="java.lang.Integer">
        SELECT `id`, `userId`, `content`, `isDeleted`, `isTop`, `reviewStatus`, `createdAt`, `updatedAt`
        FROM Messages
        WHERE userId = #{userId}
          AND isDeleted = 0
          AND reviewStatus = 'APPROVED'
        ORDER BY isTop DESC, createdAt DESC
    </select>

    <select id="getMessagesByReviewStatus" resultMap="messageMapper" parameterType="java.lang.String">
        SELECT `id`, `userId`, `content`, `isDeleted`, `isTop`, `reviewStatus`, `createdAt`, `updatedAt`
        FROM Messages
        WHERE isDeleted = 0
          AND reviewStatus = #{reviewStatus}
        ORDER BY createdAt DESC
    </select>

    <insert id="insertMessage" parameterType="icu.xindongxuanxiang.game.model.entity.Message" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `Messages`(`userId`, `content`, `isDeleted`, `isTop`, `reviewStatus`)
        VALUES(#{userId}, #{content}, 0, 0, 'PENDING')
    </insert>

    <update id="updateMessage" parameterType="icu.xindongxuanxiang.game.model.entity.Message">
        UPDATE `Messages`
        SET
            content = #{content},
            isTop = #{top},
            reviewStatus = #{reviewStatus}
        WHERE id = #{id}
    </update>

    <update id="deleteMessage" parameterType="java.lang.Integer">
        UPDATE Messages
        SET isDeleted = 1
        WHERE id = #{id}
    </update>

    <update id="deleteMessagesByUserId" parameterType="java.lang.Integer">
        UPDATE Messages
        SET isDeleted = 1
        WHERE userId = #{userId}
    </update>

    <update id="updateMessageReviewStatus">
        UPDATE Messages
        SET reviewStatus = #{reviewStatus}
        WHERE id = #{id}
          AND isDeleted = 0
    </update>

</mapper>
